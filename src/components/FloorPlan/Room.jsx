import styled from "styled-components";

const Room = styled.div`
    width: 100%;
    position: absolute;
    top: 0;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    padding: 1rem;
    pointer-events: none;
`;

export default Room;
